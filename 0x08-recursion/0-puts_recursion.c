#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - a function that prints a string
 * followed by a new line
 * @s: char variable
 * Return: string
 */
void _puts_recursion(char *s)
{
	puts(s);
}

#include "main.h"
#include <string.h>

/**
 * _strlen_recursion - returns the length of a string
 * @s: char variable
 * Return: integer
 */
int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}

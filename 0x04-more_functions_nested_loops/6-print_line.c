#include "main.h"
/**
 * print_line - a function that draws
 * a straight line
 * @n: the program variable
 * Return: Always 0
 */
void print_line(int n)
{
	if (n <= 0)
	{
		_putchar('\n');
	}
	else
	{
		_putchar(95);
	}
}

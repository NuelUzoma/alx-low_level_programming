#include "main.h"
/**
 * _islower-Find the lower case
 * @c: is the variable of the program
 * Return: Always 0
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}

}
